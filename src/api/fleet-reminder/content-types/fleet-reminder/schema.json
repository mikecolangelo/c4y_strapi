{
  "kind": "collectionType",
  "collectionName": "fleet_reminders",
  "info": {
    "singularName": "fleet-reminder",
    "pluralName": "fleet-reminders",
    "displayName": "Fleet Reminder",
    "description": "Recordatorios para vehículos (únicos o recurrentes)"
  },
  "options": {
    "draftAndPublish": false
  },
  "pluginOptions": {},
  "attributes": {
    "title": {
      "type": "string",
      "required": true
    },
    "description": {
      "type": "text",
      "required": false
    },
    "reminderType": {
      "type": "enumeration",
      "enum": ["unique", "recurring"],
      "required": true,
      "default": "unique"
    },
    "scheduledDate": {
      "type": "datetime",
      "required": true
    },
    "recurrencePattern": {
      "type": "enumeration",
      "enum": ["daily", "weekly", "biweekly", "monthly", "yearly"],
      "required": false
    },
    "recurrenceEndDate": {
      "type": "datetime",
      "required": false
    },
    "isActive": {
      "type": "boolean",
      "default": true,
      "required": true
    },
    "isCompleted": {
      "type": "boolean",
      "default": false,
      "required": false
    },
    "lastTriggered": {
      "type": "datetime",
      "required": false
    },
    "nextTrigger": {
      "type": "datetime",
      "required": true
    },
    "authorDocumentId": {
      "type": "string",
      "required": true
    },
    "vehicle": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::fleet.fleet",
      "inversedBy": "reminders"
    },
    "assignedUsers": {
      "type": "relation",
      "relation": "manyToMany",
      "target": "api::user-profile.user-profile",
      "mappedBy": "assignedReminders"
    },
    "mileageThreshold": {
      "type": "integer",
      "min": 0,
      "required": false,
      "description": "Umbral de kilometraje para activar el recordatorio (en Km)"
    },
    "mileageNotificationSent": {
      "type": "boolean",
      "default": false,
      "required": false,
      "description": "Indica si ya se envió la notificación de kilometraje alcanzado"
    },
    "notificationType": {
      "type": "enumeration",
      "enum": ["oil_change", "maintenance", "inspection", "tire_rotation", "other"],
      "default": "other",
      "required": false,
      "description": "Tipo de recordatorio basado en kilometraje"
    }
  }
}
